
// AI Integration removed as per user request.
// This file now contains dummy functions to prevent import errors.

export const generateRecipeSuggestion = async (ingredients: string[]) => {
  console.warn("AI features are disabled.");
  return null;
};

export const analyzeRecipeImage = async (base64Image: string) => {
  console.warn("AI features are disabled.");
  return null;
};

export const analyzeMultipleRecipesFromDoc = async (files: { mimeType: string, data: string }[]) => {
  console.warn("AI features are disabled.");
  return null;
};
